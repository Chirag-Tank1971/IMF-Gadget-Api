<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Gadget | Phoenix</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="overflow-hidden">

  <div class="bg-gray-50 min-h-screen flex flex-col items-center justify-center px-4">

    <div class="bg-white shadow-2xl rounded-2xl w-full max-w-lg p-10 border border-gray-200">
      <h2 class="text-3xl font-bold text-indigo-700 mb-6 text-center">Add New Gadget</h2>
  
      <form action="/gadget/addGadget" method="POST" class="space-y-5">
        <!-- Gadget Name -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Gadget Name</label>
          <input type="text" id="name" name="gadgetName" required
            class="w-full px-4 py-2 border border-gray-300 rounded-md bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500" />
        </div>
  
        <!-- Description -->
        <div>
          <label for="description" class="block text-sm font-medium text-gray-700 mb-1">Gadget Code Name</label>
          <input id="description" name="gadgetDescription" rows="3" required
            class="w-full px-2 text-xl py-2 border border-gray-300 rounded-md bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500" value="<%=codeName%>" readonly>
        </div>
  
        <!-- Status Dropdown -->
        <div>
          <label for="status" class="block text-sm font-medium text-gray-700 mb-1">Status</label>
          <select id="status" name="gadgetStatus" required
            class="w-full px-4 py-2 border border-gray-300 rounded-md bg-white focus:outline-none focus:ring-2 focus:ring-indigo-500">
            <option value="Available" selected>Available</option>
            <option value="Deployed">Deployed</option>
            <option value="Destroyed">Destroyed</option>
            <option value="Decommissioned">Decommissioned</option>
          </select>
        </div>
  
        <!-- Submit Button -->
        <div class="flex justify-center gap-6">
          <button type="submit"
            class="w-full md:w-auto px-6 py-2 bg-indigo-600 text-white rounded-full hover:bg-indigo-700 transition font-medium">
            Add Gadget
          </button>
          <a href="/gadget"
            class="w-full md:w-auto px-6 py-2 bg-red-600 text-white rounded-full hover:bg-red-700 transition font-medium">
            Cancel
        </a>
        </div>
      </form>
    </div>
  </div>

  

</body>
</html>
